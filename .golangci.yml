version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - ineffassign
    - govet
  settings:
    errcheck:
      check-type-assertions: true
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - errcheck
